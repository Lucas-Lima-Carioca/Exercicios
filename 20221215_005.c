#include <stdio.h>
#include <stdlib.h>

int main()
{
    float lado;
    printf("Informe o lado do quadrado: ");
    scanf("%f", &lado);
    printf("A area do quadrado %c: %.2f\n",130,lado*lado);
    printf("O dobro da area do quadrado %c: %.2f",130,lado*lado*2);


    return 0;
}
